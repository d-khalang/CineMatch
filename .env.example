# CineMatch AI — Environment Configuration Example
# NOTE: In CineMatch standalone architecture (v1.1.0), all credentials are provided
# directly by users at runtime through the application UI and kept in session memory.
# Do NOT store real API keys or tokens in this file or any .env file.
